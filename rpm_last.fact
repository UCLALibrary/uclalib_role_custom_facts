#!/bin/bash
OUTPUT=`rpm -qa --last | head -n 1 | cut -f 1 -d ' ' --complement | sed -e 's/^[ \t]*//'`

if [ -z "$OUTPUT" ]; then
  echo "No results"
  exit
else
  echo "[last_rpm_update]"
  echo "$OUTPUT"
fi

