#!/bin/bash
OUTPUT=`rpm -qa | grep httpd | head -n 1`

if [ -z "$OUTPUT" ]; then
  cat <<EOF
{
  "apache_install_status": {
    "pkg_name" : "None"
  }
}
EOF
else
  cat <<EOF
{
  "apache_install_status": {
    "pkg_name" : "${OUTPUT}"
  }
}
EOF
fi
