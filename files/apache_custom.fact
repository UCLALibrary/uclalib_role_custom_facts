#!/bin/bash
OUTPUT=`yum -q list installed httpd | tail -1 | awk '{ print $2 }'`

if [ -z "$OUTPUT" ]; then
  cat <<EOF
{
  "apache_install_status": {
    "httpd_pkg_name" : "None"
  }
}
EOF
else
  cat <<EOF
{
  "apache_install_status": {
    "httpd_pkg_name" : "httpd-${OUTPUT}"
  }
}
EOF
fi
